#include<iostrem>
#include<vector>
#include<maths.h>
 
using namespace std;
 
int main()
{
	int t,i=0;
	cin>>t;
        vector <int> ans(t);
        while(i<t)
        {
        	int a;
        	ans[i]=sqrt(a);
        	i++;
        }
        for(i=0;i<t;i++)
        { cout<<ans[i]<<" ";
        }
}